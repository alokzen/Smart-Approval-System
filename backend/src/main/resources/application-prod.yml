spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:smart_approval_db}?useSSL=true&serverTimezone=UTC&requireSSL=true
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
    com.smartapproval: INFO

file:
  storage:
    base-path: ${FILE_STORAGE_PATH:/var/smart-approval/storage}

